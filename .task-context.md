## Task

Refactor functions in src/config/config.ts to reduce cyclomatic complexity to ≤10.

## Files Changed

- `src/config/config.ts` - Extracted helper functions to reduce cyclomatic complexity

### Functions Refactored

1. **validateRsyncOptions** (17→10)
   - Extracted `transformBwLimit` for bw_limit validation
   - Extracted `transformTimeoutSeconds` for timeout validation
   - Extracted `isDangerousRsyncArg` and `validateRsyncExtraArgs` for extra_args validation

2. **transformDaemonSchedules** (11→4)
   - Split into `transformDaemonPrimarySchedules` (3 props)
   - Split into `transformDaemonSecondarySchedules` (2 props)

3. **transformDaemonLimits** (13→2)
   - Split into `transformDaemonPrimaryLimits` (3 props)
   - Split into `transformDaemonSecondaryLimits` (3 props)

4. **transformDaemonCore** (15→2)
   - Split into `transformDaemonTiming` (4 props)
   - Split into `transformDaemonProvider` (3 props)

5. **transformDaemonAnalysis** (15→2)
   - Split into `transformDaemonQueue` (3 props)
   - Split into `transformDaemonDecay` (4 props)

6. **validateDaemonConfig** (12→4)
   - Extracted `validateRatio` for 0.0-1.0 range validation
   - Extracted `validateOptionalCronSchedule` for optional schedules
   - Extracted `validateDaemonCoreInts` for integer validation
   - Extracted `validateDaemonSchedules` for schedule validation
   - Extracted `validateDaemonLimits` for limit validation
   - Extracted `validateDaemonThresholds` for threshold validation

7. **loadConfig** (12→7)
   - Extracted `validateConfigExtension` for extension validation
   - Extracted `readConfigFile` for file reading
   - Extracted `parseYamlContent` for YAML parsing

## Validation Commands Run

```
$ npx oxlint -c /tmp/oxlint-complexity-check.json src/config/config.ts 2>&1
Found 0 warnings and 0 errors.
```

```
$ npm test -- --run src/config/config.test.ts 2>&1 | tail -10
✓ src/config/config.test.ts (113 tests) 30ms

Test Files  1 passed (1)
Tests  113 passed (113)
```

```
$ npx oxlint -c .oxlintrc.json src/config/config.ts 2>&1
Found 0 warnings and 0 errors.
```

## Decisions Made

- Split functions by logical groupings (schedules, limits, thresholds, etc.)
- Each sub-function handles ≤4 properties to stay under complexity limit
- Used composition pattern: main functions delegate to sub-functions
- Kept all validation behavior identical to preserve functionality

## Warnings for Verifier

- Pre-existing TypeScript errors exist in the codebase (not introduced by this change)
- The TS error at line 259 is the same error as line 182 in the original (shifted due to new code)
- All 113 config tests pass, confirming no behavior changes
